import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.proyecto2',
  appName: 'proyecto2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
